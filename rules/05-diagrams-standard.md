---
description: All diagrams use PlantUML, never Mermaid or ASCII
alwaysApply: true
---

# Diagrams

PlantUML only. Every doc with flow/architecture/data change MUST have diagrams. Use `/diagrams` skill for style guide.
Obsidian: ` ```plantuml ` blocks, never PNG. GitHub: encode via `plantuml_encode.py`. Always EN. Base: `!theme plain`, `skinparam backgroundColor #FEFEFE`.
