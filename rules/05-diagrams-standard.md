---
description: All diagrams use PlantUML, never Mermaid or ASCII
alwaysApply: true
---

# Diagrams

PlantUML for Obsidian/Confluence. Mermaid for GitHub PRs (native rendering). Every doc with flow/architecture/data change MUST have diagrams. Use `/diagrams` skill for style guide.
Obsidian: ` ```plantuml ` blocks, never PNG. GitHub PR body: ` ```mermaid ` blocks. Always EN. Base: `!theme plain`, `skinparam backgroundColor #FEFEFE`.
